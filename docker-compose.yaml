services:
  kafka:
    image: apache/kafka:latest
    container_name: kafka_fishing_etl
    ports:
      - "9092:9092"
    environment:
      # These settings allow your Python script to talk to Kafka on localhost:9092
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qk"

# version: "3"

# services:
#   kafka:
#     image: apache/kafka:latest
#     container_name: kafka_fishing_etl
#     ports:
#       - "9092:9092"
#     environment:
#       KAFKA_NODE_ID: 1
#       KAFKA_PROCESS_ROLES: broker,controller
#       KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
#       KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
#       KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
#       KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
#       KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
#       CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qk"

#   airflow:
#     image: apache/airflow:2.9.0
#     container_name: airflow_fishing
#     depends_on:
#       - kafka
#     ports:
#       - "8080:8080"
#     environment:
#       AIRFLOW__CORE__EXECUTOR: SequentialExecutor
#       AIRFLOW__CORE__LOAD_EXAMPLES: "false"
#       AIRFLOW__CORE__FERNET_KEY: "test_key"
#     volumes:
#       - ./airflow/dags:/opt/airflow/dags
#       - ./:/opt/project
#     command: >
#       bash -c "
#       airflow db init &&
#       airflow users create --username admin --password admin --firstname admin --lastname user --role Admin --email admin@test.com &&
#       airflow webserver & airflow scheduler
#       "
